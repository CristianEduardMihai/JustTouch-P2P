version: '3.8'

services:
  justtouch-server:
    build: .
    container_name: justtouch-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production

